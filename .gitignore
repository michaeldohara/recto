# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Node / frontend
node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Rust / Tauri build outputs
src-tauri/target/
target/
**/*.rs.bk
Cargo.lock.bak

# OS junk
Thumbs.db
ehthumbs.db
Desktop.ini

# Environment / secrets (no expected use, but seatbelt)
.env
.env.local
.env.*.local
secrets.json

# Claude Design browser-save dumps — these embed the user's account
# UUIDs + work email in <meta> tags and a window.__OMELETTE_ME__
# JSON block. The real design source belongs in docs/design/source/
# from the handoff bundle, not from "Save Page As" exports.
docs/design/recto.html
docs/design/recto_files/
